[build]
  command = "bun run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "22"
  BUN_VERSION = "1.3.9"

# Static export ships its own 404.html; a single-route app needs no redirects.
